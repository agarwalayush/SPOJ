#include <iostream>

int main()
{
	int noStates = -1;
	int tollLimit = -1;
	while(noStates != 0 && tollLimit != 0)
	{
		// read first line (4 7)
		cin << noStates;
		cin << tollLimit;
		int timeLimits[noStates][noStates];
		int tollAmounts[noStates][noStates];
		
	}
	return 0;
}